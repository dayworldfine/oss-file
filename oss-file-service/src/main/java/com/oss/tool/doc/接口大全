user:
    1.查询用户列表
Login:
    1.注册账号(普通用户)
    2.登录(普通用户)
    3.发送验证码(普通用户)
zone:
    1.查看分区列表(普通用户)
    2.添加分区(超级管理员)
    3.删除分区(超级管理员)
    4.查看我所拥有的分区及分区密匙
file:
    1.查看文件列表(普通用户)
    2.上传文件(管理员)(上传员)
    3.删除文件(管理员)
    4.下载文件(普通用户)(游客)  未 待联调 文件属性+1
document:
    1.预览文件(普通用户)
    2.刷新凭证(普通用户)
role:
    1.查询角色列表 (超级管理员)
    2.给用户分配角色 (超级管理员) (不可分配超级管理员)
    3.查询分区列表 (超级管理员)
    4.给用户分配区域(超级管理员)
    5.输入分区密钥
    6.输入角色密钥


角色：
    角色为固定的
    1.超级管理员
    2.管理员
    3.上传员
    4.普通角色

